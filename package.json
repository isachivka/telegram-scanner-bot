{
  "name": "telegram-scanner-bot",
  "version": "1.1.1",
  "private": true,
  "description": "Self-hosted Telegram bot that turns any SANE/eSCL network scanner into a scan-to-PDF service and any CUPS printer into a print-from-chat service",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "check": "node dist/index.js --check",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@grammyjs/conversations": "^2.1.1",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "async-mutex": "^0.5.0",
    "grammy": "^1.46.0",
    "pino": "^10.3.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^24.0.0",
    "eslint": "^9.0.0",
    "pino-pretty": "^13.0.0",
    "prettier": "^3.9.6",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.0.0",
    "vitest": "^4.0.0"
  }
}
